<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="wrapper">
        <header>Todo App</header>
        <form action="/" method="post" class="inputField">
            <input type="text" name="todo" placeholder="Add your new todo">
            <button type="submit" id="add">+</button>
        </form>
        <ul class="todoList">
            <% for( let index = 0; index < listAmmount; index++ ) { %>
                <div>
                    <li>
                        <p>
                            <%= list[index] %>
                        </p>
                        <form action="/remove" method="post">
                            <button type="submit" name="remove" value="<%= index %>">Remove</button>
                        </form>
                    </li>

                </div>

                <% } %>
        </ul>
        <div class="footer">
            <span>You have <span class="pendingTasks">
                <% if ( listAmmount == 0 ) { %>
                    0
                <% } else { %>
                    <%= listAmmount %> 
                <% } %>
                </span> pending tasks</span>
            <form action="/clear" method="post">
                <button type="submit">Clear All</button>
            </form>
        </div>
    </div>
</body>

</html>

</body>

</html>